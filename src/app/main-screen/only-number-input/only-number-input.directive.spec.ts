import { OnlyNumberInputDirective } from './only-number-input.directive';

describe('OnlyNumberInputDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumberInputDirective();
    expect(directive).toBeTruthy();
  });
});
